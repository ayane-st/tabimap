<% users.each do |user| %>
  <div>
    <% users.each do |user| %>
      <%= image_tag user.get_profile_image(50,50)   %>
      <%= user.name %>
      <%= link_to 'Show', user, class: "user_#{user.id}" %>
    <% end %>
<% end %>