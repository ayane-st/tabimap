<% if obj.errors.any? %>
  <ul class="alert alert-danger" role="alert">
    <p><%= obj.errors.count %>件のエラーが発生しました</p>
    <ul>
      <% obj.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>
  </ul>
<% end %>